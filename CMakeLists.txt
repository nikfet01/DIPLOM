cmake_minimum_required(VERSION 3.2) # Проверка версии CMake.
# Если версия установленой программы
# старее указаной, произайдёт аварийный выход.

project(DIPLOM) # Название проекта

add_subdirectory(OldThreat)